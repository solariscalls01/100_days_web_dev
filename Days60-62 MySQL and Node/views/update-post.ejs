<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('includes/head', { title: 'Update post' }) %>
  <link rel="stylesheet" href="/styles/forms.css">
</head>

<body>
  <%- include('includes/header') %>
  <main>
    <h1>Update post</h1>
    <form action="/posts/<%= post.id %>/edit" method="POST">
      <div class="form-control">
        <label for="title">Title</label>

        <!-- IN ORDER TO DISPLAY the items to the "edit page", we need to get the items from the database first in order to output the items to the input box. 
        We set the values accordingly to display in the input boxes when we click that edit button -->
        <input type="text" id="title" value="<%= post.title %>" name="title" required>
      </div>
      <div class="form-control">
        <label for="summary">Summary</label>
        <input type="text" id="summary" value="<%= post.summary %>" name="summary" required maxlength="255">
      </div>
      <div class="form-control">
        <label for="content">Post Content</label>
        <!-- For textarea, the value needs to be between the <textarea>"value here"</textarea> -->
        <textarea id="content" name="content"  required rows="5"><%= post.body %></textarea>
      </div>
      <button class="btn">Update Post</button>
    </form>
  </main>
</body>

</html>